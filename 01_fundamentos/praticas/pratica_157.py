#pratica 157
def calcular_soma(lista):
    soma = 0
    for i in range(len(lista)):
        soma = soma + lista[i]
    return soma
resultado = calcular_soma([10, 20, 30, 40])
print(resultado )
#pratica 157 nova tentativa
def calcular_soma(lista):
    soma = 0
    for i in range(len(lista)):
        soma = soma + lista[i]
    return soma 
resultado = calcular_soma([5, 15, 25, 35])
print(resultado)